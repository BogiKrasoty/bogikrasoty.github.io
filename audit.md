# Аудит сайта «Боги красоты»

**Дата изучения:** 5 сентября 2026
**Объект:** корень проекта `/Users/macboochek/Documents/Ногти/god-beauty`
**Стек:** Next.js 16.3.4 + Tailwind v4, контент — markdown в `content/`, рендер на сервере из файлов.
**Метод:** построчное ревью всех страниц/компонентов, контент-файлов и конфигов; сверка с утверждённым чеклистом структуры сайта (30 разделов).

**Легенда:** 🔴 блокер (ломает сценарий/запуск) · 🟠 важно · 🟡 улучшение

---

## 0. Карта: маршруты vs чеклист vs контент

| Маршрут | Страница | Контент |
|---|---|---|
| `/` | ✅ | 🟠 (секции есть, аудит ниже) |
| `/services` | ✅ | ✅ 6 направлений + 18 услуг (цены `# TODO`) |
| `/services/[category]` | ✅ 6: nails/makeup/brows/lashes/permanent/tattoo | ✅ реальный контент |
| `/services/[category]/[slug]` | ✅ | ✅ 18 услуг |
| `/courses` | ✅ | ✅ каталог 22 |
| `/courses/[category]` | ✅ (5: base/advanced/beauty/pmu/tattoo) | ✅ структура, 🔴 содержание тонкое |
| `/courses/[category]/[slug]` | ✅ | 🔴 10 из 22 курсов без тела |
| `/teachers` | ✅ | 🔴 1 фейковый мастер |
| `/teachers/[slug]` | ✅ | 🔴 1 фейковый |
| `/portfolio` | ✅ | 🔴 1 пример, фото отсутствует |
| `/portfolio/[slug]` | ✅ | 🔴 то же |
| `/about` | ✅ | 🟠 |
| `/contact` | ✅ | 🟠 форма работает, контакты — заглушки (1.2) |
| `/contacts` | ✅ слинкован из Header/Footer | 🟠 дубль `/contact` не решён (1.10) |
| `/privacy` | ✅ | 🟠 |
| `/consent` | ✅ | 🟠 тексты требуют проверки юристом |
| `/reviews`, `/faq`, `/portfolio/nails…` | ❌ | ❌ |
| `404`, `error`, `sitemap.xml`, `robots.txt` | ✅ (06.09.2026) | ✅ |

---

## 1. Кросс-страничные блокеры (важнее всего)

### 🟠 1.1. Форма записи (✅ реализована 06.09.2026, нужна SMTP-настройка)
Раньше: `app/contact/page.tsx` имел `action="/send.php"` + `preventDefault` и фейковый success без единого запроса. `send.php` Next не обслуживает.
**Теперь:**
- `app/api/lead/route.ts` (POST, nodejs-runtime): валидация полей, honeypot-поле `company` (боты получают фиктивный success), rate-limit 6/час по IP, маппинг направления → русская подпись, логирование при ошибке. Проверено на живом сервере: невалид → 400, honeypot → 200, без конфигурации почты → честный 501 `MAIL_NOT_CONFIGURED`, GET → 405.
- Доставка `lib/mail.ts` без новых npm-зависимостей: SMTP-клиент на `net`/`tls` (EHLO, STARTTLS/SSL, AUTH PLAIN, DATA c base64-UTF8 subject/body) ЛИБО webhook (`LEAD_WEBHOOK_URL`). Протокольный тест пройден против локального SMTP; сквозной тест через роут: письмо на `bud-bud777@mail.ru` с кириллицей доставлено.
- Клиент формы: реальный `fetch('/api/lead')`, состояния sending/success/error с блоком «Не удалось отправить…», кнопка-дизейбл.
- Конфигурация — `.env.example` (SMTP_HOST/PORT/SECURE/USER/PASS или LEAD_WEBHOOK_URL, LEAD_TO по умолчанию `bud-bud777@mail.ru`). `.env*` в .gitignore.
**Осталось:** заполнить реальные SMTP-данные (или webhook) в `.env.local` и проверить доставку в проде.

### ✅ 1.2. Контактные данные — реальные (06.09.2026)
- Телефон `+7 (968) 307-82-57` (`tel:+79683078257`) — Header, Footer, главная, `/services`, `/contact`, `/contacts`.
- Почта `bud-bud777@mail.ru` — та же, что приём заявок (send.php/`/api/lead`); проставлена в Footer, `/contact`, `/contacts`, `/privacy`, `/consent`.
- Адрес: «Москва, ул. Осенняя, д. 23, 10 этаж, офис 21» — на `/contacts`.
- Карты: Яндекс Карты — точный листинг `https://yandex.ru/maps/-/CTXwz0Yn`; 2ГИС — точная карточка `https://2gis.ru/moscow/firm/70000001113454257`.
- Единый источник: `lib/contacts.ts`.
**Осталось:** Telegram-аккаунт («уточнить») — не подключали. Заглушек `+7 000…`/`info@god-beauty.ru`/`ул. Примерная` в коде не осталось (grep пуст).

### ✅ 1.3. SEO-каркас (06.09.2026)
- `app/sitemap.ts` → `sitemap.xml` на `https://god-beauty.ru/sitemap.xml`: главная, `/services` (+6 категорий +18 услуг), `/courses` (+5 категорий +22 курса), `/teachers` (+мастера), `/portfolio` (+работы), `/about`, `/contact`, `/contacts`, `/privacy`, `/consent`. Проверено: отдаёт полный список.
- `app/layout.tsx`: `metadataBase` из `lib/seo.ts` (`NEXT_PUBLIC_SITE_URL` или `god-beauty.ru`). Canonical добавлен на все страницы через `alternates` (разрешается относительно metadataBase — проверено на живом сервере: Home `…/`, `/services`, `/contact`, `/courses/permanent`, `/services/nails`, карточки курсов). Из корневого layout canonical убран, чтобы не штамповать главную на каждой странице.
- Яндекс.Метрика: фейковый `ym(0)` убран. Скрипт и `<noscript>`-пиксель подключаются только при `NEXT_PUBLIC_METRIKA_ID` в env (`app/layout.tsx:63-81`); без ID — скрипта в выводе нет (проверено curl). Данные о cookies/аналитике добавлены в `/privacy`.

### ✅ 1.4. Системные страницы (06.09.2026)
- `app/not-found.tsx` — брендовая 404: hero-glow, eyebrow «404», gold-rule, CTA «На главную / Услуги / Обучение». Проверено: `/404xyz` → заголовок «Страница не найдена», статус 404.
- `app/error.tsx` — клиентская брендовая страница ошибки с кнопкой «Попробовать снова» (reset) и CTA «На главную».
- `app/consent/page.tsx` — «Согласие на обработку персональных данных» (чеклист п.28): цель, перечень данных, действия, передача, срок, отзыв, ссылка на политику. Линки: форма записи → согласие + политика; Footer → обе страницы; `/privacy` ссылается на согласие.

### 🟠 1.5. Таксономия URL не совпадает с чеклистом (УСЛУГИ ✅ 06.09.2026)
Чеклист зафиксировал: `/services/nails`, `/services/makeup`, `/services/brows`, `/services/lashes`, `/services/permanent`, `/services/tattoo`; категории курсов `/courses/permanent`, `/courses/tattoo`.

**Услуги — исправлено (06.09.2026).** Внедрена модель «внутренний slug папки ≠ публичный URL»:
- реестр `content/services/_index.md` содержит для каждой категории `title / slug / contentSlug / description`;
- страницы и ссылки строят url по публичному `slug`: `/services/nails`, `/services/permanent`, …;
- убраны hardcoded `categories`/`categoryNames`/`categoryNotes`/`directions`; `getAllServices()` ходит по `content/services/{contentSlug}/{service}.md`, игнорирует корневые и `_`-файлы;
- `Service.categorySlug` = публичный слаг; `generateStaticParams` для SEO;
- проверено: на всех страницах линки только публичные, `/services/nail-service` → 404, дубликатов категорий нет, build 36/36.
Замечание из 1.8 (пустые категории и самозамыкание ссылок) закрыто.

**Курсы — исправлено (06.09.2026).** Категории приведены к чеклисту: `/courses/permanent` и `/courses/tattoo` вместо единой `pmu`. Папка `content/courses/pmu` переименована в `permanent` (front-matter `category: permanent`), тату-курс выделен в `content/courses/tattoo/tattoo-base.md` (`category: tattoo`). `/courses` и `/courses/[category]` отражают 5 групп; `generateStaticParams` добавлен на обе динамические страницы курсов (build 64/64, SSG, 22 курса + 5 категорий). Проверено: `/courses/permanent|tattoo` и их курсы → 200; старые `/courses/pmu` и `/courses/pmu/tattoo-base` → 404; в `/courses/permanent` только `pmu-full`.

### ✅ 1.6. Часы работы убраны (06.09.2026)
Четыре набора часов (Header meta, `/about`, `/contact`, `/contacts`) удалены полностью:
- Header meta-bar: вместо часов — линк «Записаться» → `/contact` (`components/Header.tsx:25`).
- `/about` — убран пункт «Режим 09:00–20:00» (`app/about/page.tsx`).
- `/contact` — убран пункт «Режим» в aside (`app/contact/page.tsx`).
- `/contacts` — убран пункт «Режим Пн–Пт…» (`app/contacts/page.tsx`).
Проверено grep-ом по всем страницам: упоминаний 09:00/20:00/10:00/18:00/«режим» нет.

### ✅ 1.7. Фейковые данные — частично убраны (06.09.2026)
- Адрес «ул. Примерная, 1» заменён на реальный «ул. Осенняя, д. 23» (1.2).
- Телефон/почта/соцсети-заглушки заменены реальными (1.2/19).
- Фейк «Иван Иванов» удалён; вместо него — 5 мастеров (драфт-состав, 06.09.2026, см. 1.14).
**Осталось (контент):**
- Имена/био мастеров — драфт, заменить на реальные (см. 1.14).
- Фото мастеров/работ отсутствуют — везде монограммные обложки (см. 1.14, 1.15).
- Stats на `/about`: «10+ лет опыта», «100+ выпускников» (`app/about/page.tsx:13-17`) — без подтверждения; `/teachers` meta «Опыт: от 5 лет» жёстко (`app/teachers/page.tsx:28`).

### 🟠 1.8. Контент услуг (✅ 06.09.2026)
Раньше: `getAllServices` давал корневым файлам `category: ''`, категории были пустые, ссылка на услугу на `/` и `/services` замыкалась на `/services`.
**Исправлено:** услуги лежат в `content/services/{contentSlug}/{service}.md` по модели каталога — 18 услуг в 6 категориях; старый корневой `nail-service.md` удалён (см. 1.5). Цены/длительность там, где точных данных нет, помечены `# TODO: цена — согласовать прайс` / `# TODO: длительность` и **скрыты** на страницах (не выдуманы). YAML-описания с `: ` взяты в кавычки (ограничение plain-scalar). Каталог обучения (22 курса с реальными ценами) не трогали.

### ✅ 1.9. Курсы: 10 из 22 — пустышки (заполнены 06.09.2026)
Раньше по байтам (`wc -c`): `beauty/lamimaker` (94б), `makeup-for-self` (102б), `lamimaker-brow` (111б), `led-lash` (113б), `profession-makeup-artist` (113б), `advanced/sales-technique-service` (119б), `beauty/lash-maker` (146б), `professional-brow-maker` (149б), `pmu/tattoo-base` (156б), `pmu/pmu-full` (157б) — только front-matter без description/features/тела. Многие `advanced/*` имели лишь один пункт features.
**Исправлено 06.09.2026:** все 22 курса приведены к единой структуре — `description`, обогащённые `features` и HTML-тело `.md-body` с блоками «Для кого» / «Программа» / «Результат». Цены/длительности в front-matter сохранены без изменений; категории по файлам проверены и совпадают с папками (base 6 / advanced 7 / beauty 7 / permanent 1 / tattoo 1). Тела у нас ~200–400 слов на курс, рендер подтверждён (h2, списки).

### 🟠 1.10. Дубликат `/contact` и `/contacts`
`/contact` — страница записи (клиентская форма), слинкована во всех CTA и «Записаться».
`/contacts` — отдельная страница контактов; с 06.09.2026 слинкована из Header и Footer (см. секции 18–19).
Чеклист предполагает `/contacts` как страницу контактов + «Запись» как modal/форма. Суть не закрыта: нужно решить, остаются ли две страницы (запись vs контакты) или `/contacts` перехватывает навигацию/редирект, и наполнить `/contacts` реальными данными.

### 🟡 1.11. Гуттеры: header и контент живут на разных сетках
Header — full-width: контент от края `px-14` = 56px при xl (`components/Header.tsx:22,30`). Контент внутренних страниц — `max-w-7xl mx-auto px-6` (на 1440 отступ от края ≈ 104px). Вертикали текста header и страницы не совпадают — придирчиво заметно на десктопных страницах.

### 🟡 1.12. CMS (Sveltia) не работает в текущем виде
`sveltia.config.yml` есть (git backend, `branch: main`), но:
- маршрут `/admin` не подключён (нет `app/admin/`);
- схема коллекции `courses` пишет в `content/courses/` **плоско** и не создаёт категории, а каталог `/courses` показывает только файлы в известных подпапках (`base/advanced/beauty/pmu`) — файл, созданный CMS в корне `content/courses/`, получит `category: ''` и не появится в каталоге (потеря контента);
- схема не включает многие поля страниц (features, course.teacher, service.duration и пр.);
- git-репозиторий есть (проверено: `.git` присутствует) — этот пункт ок, но всё остальное требует донастройки.

### ✅ 1.13. Отзывы и FAQ — живой контент (06.09.2026)
Созданы коллекции и рендер:
- `content/reviews/*.md` (6 шт., поля `name/service/text`) → секция «Слова наших клиентов» на главной (`app/page.tsx`), editorial-цитаты в сетке.
- `content/faq/*.md` (8 шт., поля `question/answer`) → секция FAQ на главной с аккордеоном `components/FaqSection.tsx` (клиентский, chevron/плюс).
- Типы `Review`/`FaqItem` (`lib/types.ts`), геттеры `getAllReviews()`/`getAllFaq()` (`lib/content.ts`).
Контент — драфт от лица клиентов/школы: перепроверить тексты и факты до публикации.

### 🟠 1.14. Мастера: драфт-состав вместо фейка (06.09.2026)
`ivan-ivanov.md` удалён, добавлено 5 мастеров (`content/teachers/<\*>`): Анна Соколова (ногти), Екатерина Лебедева (брови), Мария Орлова (ресницы), Дарья Морозова (визаж), Ирина Смирнова (ПМ/тату) — поля `name/specialization/bio`, без фото и без личных соцсетей (не выдумывали). Главная и `/teachers` рендерят биографию; страница мастера показывает его работы из портфолио по совпадению `teacher: <имя>` (`app/teachers/[slug]/page.tsx`).
**Нужно:** реальные имена/фото/соцсети; фильтр по направлениям на `/teachers` (чеклист п.17); связь «Курсы преподавателя» не реализована (чеклист п.18).

### 🟠 1.15. Портфолио: 6 работ + сетка (06.09.2026)
`example.md` удалён; добавлено 6 работ (`content/portfolio/*.md`: мрамор, френч, брови, объём 2D, вечерний визаж, пудровые брови) с полями `title/category/teacher/description` и **без** image → везде монограммная обложка (фото нет). `/portfolio` теперь: featured-работа (first) + сетка «Все работы» 3×2 (`app/portfolio/page.tsx`); страница мастера дублирует соответствующие работы.
**Нужно:** реальные фото работ («до/после»), фильтры по направлениям/мастерам (чеклист п.19), просмотр фото в большем размере, переключение «предыдущая/следующая» (п.20).

---

## 2. Главная `/` (`app/page.tsx`)

- ✅ **Воронка услуг работает (06.09.2026):** «Направления» ведут на `/services/nails|makeup|brows|lashes|permanent|tattoo`; «Что мы предлагаем» — на конкретные услуги `/services/{ctg}/{slug}` (см. 1.5/1.8).
- ✅ **Отзывы и FAQ на главной (06.09.2026, см. 1.13):** «Слова наших клиентов» (6 цитат в сетке, чеклист п.11) и аккордеон FAQ (8 вопросов, чеклист п.22).
- 🟠 **Нет секции «Преимущества / почему мы»** (чеклист п.10); «Кратко о „Богах красоты“» (п.2) — на `/about`.
- 🟠 **Портфолио-обложка** (`app/page.tsx`): `content/portfolio/*.md` без image → всегда монограммная fallback-панель «БК». Заголовок «Работы, которые говорят» обещает результат, фото нет (см. 1.15).
- 🟠 **Мастера** (`app/page.tsx`): 5 мастеров-драфтов с биографиями; фото нет — монограммы с инициалами (см. 1.14).
- ✅ **Финальный CTA** (`app/page.tsx`): реальные телефон/почта (1.2), «Записаться» → `/contact`.
- 🟡 Hero: заголовок CTA «Записаться» и строки meta (`app/page.tsx:102-105`) дублируют meta в Header (визуальный двухэтажный повтор «Москва · по записи»).

## 3. `/services` (`app/services/page.tsx`)

- ✅ Каталог: 6 направлений из реестра + 18 услуг с ссылками на конкретные страницы (06.09.2026).
- 🟡 Цены скрыты, пока `# TODO` не заполнены — согласовать формулировку «точную цену мастер назовёт после консультации».
- 🟡 meta «Направлений» теперь из реестра (не хардкод).
- 🟡 Кнопка телефона — заглушка (1.2).

## 4. `/services/[category]` (`app/services/[category]/page.tsx`)

- ✅ Категории наполнены (06.09.2026); `generateMetadata` берёт title/description из реестра.
- 🟡 Нет breadcrumbs. Empty-state «Скоро здесь появятся работы» остаётся только для пустых на будущее — сейчас не встречается.

## 5. `/services/[category]/[slug]` (`app/services/[category]/[slug]/page.tsx`)

- ✅ Страница достижима с контентом (06.09.2026): 18 услуг по публичным URL; PageHeader с «Направление»/«Стоимость» (по запросу, пока нет цены), обложка или md-тело, CTA.
- 🟠 Скудна vs чеклист п.9: нет «Что входит», «Для кого», «Длительность» (модель `Service` в `lib/types.ts:1-10` не имеет полей duration/features), «Примеры работ», «Мастер/мастера», «Форма записи», «FAQ».
- ✅ Мета «Направление» = title категории из реестра (не транслит).

## 6. `/courses` (`app/courses/page.tsx`)

- ✅ Структура пяти групп и нумерация `01 / 01.01` корректны, CTA-блок осмысленный.
- ✅ Категории приведены к чеклисту (06.09.2026): `/courses/permanent` и `/courses/tattoo` разделены (см. 1.5).
- 🟠 Счётчик групп: `group.index + 1` (`app/courses/page.tsx:69`) — зависит от порядка массива, при отсутствии категории сдвигается (косметика, но хрупко).
- 🟡 Пустые курсы (1.9) показывают строку «1 день» как дефолт `course.duration ?? '1 день'` (`app/courses/page.tsx:99`) — может оказаться ложным.

## 7. `/courses/[category]` (`app/courses/[category]/page.tsx`)

- ✅ Работает (SSG с 06.09.2026). 🟠 Нет breadcrumbs; нет «→ все категории»-навигации обратно/между категориями (только список курсов).

## 8. `/courses/[category]/[slug]` (`app/courses/[category]/[slug]/page.tsx`)

- 🔴 Страница-мини-лендинг по чеклисту (п.16 — «очень важная»): реализовано PageHeader + «Входит в курс» (features) + md-тело (`description` + «Для кого/Программа/Результат», с 06.09.2026 есть у всех 22 курсов, см. 1.9) + CTA. **Отсутствуют:** breadcrumbs, категория, «Что изучите», программа по дням, формат обучения, практика на моделях, «Что входит в стоимость», сертификат, бонусы, преподаватель, фото с обучения, отзывы учеников, FAQ, форма заявки на странице.
- 🟠 Дефолтная заглушка курса — монограмма «A» (`app/courses/[category]/[slug]/page.tsx:49`) на всех курсах без фото.
- ✅ «За 6 дней» подтверждено бенефициаром 06.09.2026: `content/courses/base/master-manicure-6-days.md` везде «6 дней» (title/duration/description/тело) — формулировка «За 7 дней» из раннего брифа отменена, ничего менять не нужно.

## 9. `/teachers` (`app/teachers/page.tsx`)

- ✅ Название приведено к чеклисту: «Мастера» (Header/Footer/страница, 06.09.2026).
- ✅ 5 мастеров вместо фейка (06.09.2026, см. 1.14): имена/специализации/био из front-matter; списком из `content/teachers`.
- 🟠 meta «Мастеров: (length || 1)» + «Опыт: от 5 лет» (`app/teachers/page.tsx:27-29`) — магическая цифра без источника; «от 5 лет» стоит заменить на реальные данные команды.
- 🟠 Нет фильтра по направлению (чеклист п.17); «Направления команды» дублируют линки на `/services/*` (см. 1.5).

## 10. `/teachers/[slug]` (`app/teachers/[slug]/page.tsx`)

- ✅ Страница живого мастера (06.09.2026, см. 1.14): био из front-matter, «Работы мастера» подтягивают портфолио по `teacher: <имя>`.
- 🟠 Чеклист п.18: нет «Курсы преподавателя» (связь teacher→course не реализована), нет фото/реального портфолио мастера.
- 🟡 `social` рендерится просто текстовыми ссылками без иконок (`app/teachers/[slug]/page.tsx:71-83`) — стилистически выпадает из остального сайта.

## 11. `/portfolio` (`app/portfolio/page.tsx`)

- ✅ Сетка работ добавлена (06.09.2026, см. 1.15): featured-работа (first) + grid «Все работы» 3×2; монограммные обложки вместо фото.
- 🟠 Фото работ отсутствуют — монограммы (см. 1.15). Нет фильтров по направлениям/мастерам (чеклист п.19), нет просмотра фото в большем размере.
- 🟡 meta «Работы: обновляются» — хардкод.

## 12. `/portfolio/[slug]` (`app/portfolio/[slug]/page.tsx`)

- ✅ Схема нормальная. 🟠 Нет breadcrumbs; нет переключения «предыдущая/следующая работа»; фото фолбек. Нет страниц `/portfolio/nails` и др. (чеклист п.20).

## 13. `/about` (`app/about/page.tsx`)

- ✅ Хорошая структура (стори/статы/миссия/ценности).
- 🟠 Stats без подтверждений («10+ лет», «100+ выпускников», «6+ направлений»).
- 🟠 Показан «Режим» (см. 1.6).
- 🟡 Чеклист п.21: нет «Пространство» (фотографии интерьера), нет «Команда» как факта, нет «Экспертиза/подход к обучению» развёрнуто; фотографий на странице нет вообще.

## 14. `/contact` (`app/contact/page.tsx`) — форма записи

- ✅ Форма отправляет через `POST /api/lead` с состояниями sending/success/error (06.09.2026, см. 1.1); block ошибки «Не удалось отправить…» + повторная попытка. Доставка зависит от SMTP/webhook-конфигурации.
- 🟠 Нет явного consent-чекбокса — только текст «Отправляя форму, вы соглашаетесь…» с линками на согласие и политику (`app/contact/page.tsx`); юридически слабее чекбокса.
- 🟠 Нет выбора «Услуга или курс» (есть select «Направление» с пунктом «Обучение» — приемлемо, но поля «услуга/курс» нет).
- 🟠 Aside: телефон/почта-заглушки (1.2).
- ✅ Honeypot + rate-limit добавлены на сервере (антиспам).

## 15. `/contacts` (`app/contacts/page.tsx`)

- 🟠 Орфан устранён (06.09.2026): слинкована из Header и Footer, реальные контакты, карта, соцсети. Дубликат с `/contact` не решён (1.10).
- ✅ Часы убраны (1.6, 06.09.2026).
- ✅ Реальные телефон/почта/адрес (1.2); карта: встроенный iframe Яндекс (`map-widget/v1/?text=…`), кнопки «Открыть в Яндекс Картах» (листинг `- /CTXwz0Yn`) и «2ГИС» (карточка `70000001113454257`).
- ✅ Соцсети: VK `vk.com/bogy_krasoty_school`, Instagram `bogi_krasoty_school`, TikTok `@bogikrasoti`; Telegram не подключён («уточнить»).
- 🟠 Нет «Форма связи» на странице (есть отдельная `/contact`), соцсети — без 2ГИС-карточки профиля.

## 16. `/privacy` (`app/privacy/page.tsx`)

- ✅ Базовая политика; добавлен раздел «Аналитика» и ссылка на согласие (06.09.2026).
- ✅ Отдельная страница «Согласие на обработку персональных данных» добавлена (`app/consent/page.tsx`, чеклист п.28); ссылки из формы и Footer.
- 🟡 Мета «Редакция: текущая» — нет даты версии; тексты политики/согласия желательно проверить юристом.

## 17. Системное и SEO (✅ 06.09.2026)

- ✅ `app/sitemap.ts` → `/sitemap.xml` (все маршруты, приоритеты) — отдаётся и соответствует `robots.txt`.
- ✅ `app/not-found.tsx` и `app/error.tsx` — брендовые (см. 1.4).
- ✅ Метрика: нет фейкового `ym(0)`; подключается только при `NEXT_PUBLIC_METRIKA_ID` (`app/layout.tsx`).
- ✅ Canonical на всех страницах (относительный `alternates` + `metadataBase`), пер-страничные структурные данные не добавляли.
- 🟡 Ог-картинка — `logo.webp` (квадратная), пер-страничных OG нет.
- 🟡 `lib/seo.ts` `SITE_URL` из env (`NEXT_PUBLIC_SITE_URL`) — на проде проверить фактический домен.

## 18. Header (`components/Header.tsx`)

- ✅ Меню приведено к чеклисту п.31 (06.09.2026): «Услуги | Обучение | Мастера | Портфолио | О нас | Контакты» (→ `/contacts`) + «Записаться» (→ `/contact`). Footer синхронизирован. «Команда» нигде не используется.
- ✅ Часы в meta-баре убраны — вместо них линк «Записаться» (1.6, 06.09.2026).
- 🟡 Навигация уходит в бургер раньше (на lg 1024–1365), хотя места достаточно; с 6 пунктами и «Записаться» стоит перепроверить брейкпоинт.
- 🟡 Гуттеры header vs страницы (1.11). Нет `aria-current` у активного пункта.
- 🟡 Телефон — заглушка (1.2). «Записаться» → `/contact` — консистентно.

## 19. Footer (`components/Footer.tsx`)

- ✅ «Контакты» в меню ведёт на `/contacts` (06.09.2026, см. 1.10 — дубликат с `/contact` всё ещё решается).
- ✅ Реальные телефон/почта/адрес/карты (Яндекс Карты + 2ГИС) и соцсети VK/Instagram/TikTok (06.09.2026).
- ✅ Ссылки на «Политику конфиденциальности» и «Согласие на обработку данных» (06.09.2026, чеклист п.28).
- 🟡 Telegram не подключён («уточнить»).

---

## Приоритеты (обновлено 06.09.2026)

1. ✅ **Воронка услуг** — модель категорий, публичные URL, контент 6 направлений, меню «Мастера/Контакты».
2. ✅ **Форма записи** — `POST /api/lead` + SMTP/webhook-доставка (06.09.2026); осталось: реальные SMTP-данные в `.env.local`.
3. ✅ **Контактные данные** — реальные телефон/почта/адрес, Яндекс Карты (+iframe), 2ГИС, соцсети VK/Instagram/TikTok (06.09.2026). Осталось: Telegram («уточнить»).
4. ✅ **SEO/система** — sitemap.xml, canonical на всех страницах, брендовые 404/error, метрика через `NEXT_PUBLIC_METRIKA_ID`, страница «Согласие на обработку данных» (06.09.2026).
5. 🟠 **Контент (частично 06.09.2026, осталось)** — ✅ все 22 курса наполнены телами (1.9); ✅ драфт-мастера (1.14), ✅ 6 работ портфолио + сетка (1.15), ✅ отзывы + FAQ на главной (1.13). Осталось: реальные имена/фото мастеров, фото работ, персональные соцсети мастеров, связь «Курсы преподавателя», фильтры на `/teachers` и `/portfolio`, мини-лендинги курсов по чеклисту (раздел 8), фото интерьеров и подтверждённые статы на `/about`.
6. ✅ **Таксономия курсов** — `/courses/permanent` и `/courses/tattoo` разделены, `pmu` → `permanent` (06.09.2026).

---

## Pages CMS + содержание контента + маркировка Instagram (05.09.2026)

### Контент перенесён из кода в файлы (готово для Pages CMS)
- `content/site/contacts.json` — единый источник контактов / соцсетей / карт (читает `lib/contacts.ts`, производные `phoneHref`/`emailHref`).
- `content/site/home.json` и `content/site/about.json` — тексты главной и «О нас»; `app/page.tsx` и `app/about/page.tsx` читают их (about с 05.09.2026).
- `content/directions/*.md` (6 шт., `title/slug/contentSlug/description/order` 1–6) — реестр направлений вместо `content/services/_index.md` (удалён); URL и разделы услуг сохранились (`/services/nails|makeup|brows|lashes|permanent|tattoo`).
- Модель данных: у Service/Course/Teacher/PortfolioItem/Review/FaqItem добавлены `published?` и `order?` (сортировка стабильная, новые элементы — в конец); у Teacher `social` заменён на `socials: {label,href}[]` (страница мастера обновлена).
- `public/hero-girl.png` перенесён в `public/images/hero-girl.png`.

### `.pages.yml` — конфигурация Pages CMS (корень репо)
- `media`: public/images → `/images` (jpg/jpeg/png/webp/avif).
- Группы: Главная (файлы home.json/about.json, формат json), Услуги (коллекция направлений + 6 коллекций услуг по папкам), Обучение (5 коллекций курсов), Команда, Портфолио, Отзывы, FAQ, Контакты (contacts.json).
- Поля: `published` (boolean, default true), `order` (number, default 999), `slug` (`filename: "{slug}.md"`, pattern латиница), `price`/`duration`/`description`/`features`/`photo`/`socials`/`maps` и т.д.; тело md-файлов — скрытое text-поле (rich-text не используется, чтобы не переписывать HTML-тела курсов); критичные поля (`category`, `contentSlug`, `ymapsConstructor`, CTA-ссылки) readonly.
- YAML-якоря для общих схем полей услуг/курсов; файл парсится (js-yaml), линт и build чистые (SSG: 6 категорий услуг, 22 курса — как было).
- Sveltia (`sveltia.config.yml`, раздел 1.12) фактически не использовался — рабочим CMS становится `.pages.yml` + app.pagescms.org.

### Маркировка Instagram (5.09.2026)
- Все упоминания на сайте: «Instagram*» + единая сноска «* Instagram — социальная сеть, принадлежащая Meta Platforms Inc., деятельность которой признана экстремистской и запрещена на территории Российской Федерации.» рядом с каждым упоминанием:
  - Footer — под рядом иконок соцсетей (иконка с маркером `*`, рендер соцсетей обобщён по `CONTACTS.socials`);
  - `/portfolio` — в блоке «Больше работ» рядом с кнопкой;
  - `/contacts` (список соцсетей) — в том же блоке;
  - страница мастера (если у мастера есть Instagram) — в блоке соцсетей.
- Ссылки на Instagram сохранены рабочими; VK/TikTok/2ГИС/Яндекс Карты без маркировок; слова «РЕКЛАМА» не добавлялись.
- Проверено: сноска присутствует на главной, `/portfolio`, `/contacts`, страницах мастеров; lint + build зелёные.

### Дополнения по чек-листу владельца (05.09.2026)
- «Ссылка на запись» стала редактируемой: `content/site/contacts.json` → `bookingLabel`/`bookingHref` (кнопка «Записаться» в шапке и мобильном меню, `components/Header.tsx`).
- Услуги и курсы: в `.pages.yml` добавлены поля `image` («Фото услуги»/«Фото курса»); рендер уже поддерживался через `hasPublicFile` с фирменной обложкой-фолбэком (страницы деталей услуг/курсов).
- Проверен весь `.pages.yml`: каждый `file`/`collection`/`media.input` существует на диске; YAML парсится; lint + build зелёные.